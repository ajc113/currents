<!DOCTYPE html>
<html>
  <head>
    <script src="https://use.typekit.net/yzs0vln.js"></script>
    <script>
      try{Typekit.load({ async: true });}catch(e){}
    </script>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag "https://js.stripe.com/v3/" %>
    <%= csrf_meta_tags %>
    <!-- Document Title
	============================================= -->
    <title><%= full_title(yield(:title)) %></title>
  </head>
  <body>
    <div class="wrapper">
      <%= render 'layouts/navbar' %>
      <% if user_signed_in? %>
        <% if !current_user.trial_over? %>
          <div class="alert alert-info alert-dismissable">
            <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
            Your trial period will end in <%= pluralize(current_user.remaining_trial_days, 'day') %>. 
            <% if current_user.payment_source.nil? %>
              <%= link_to 'Add a payment source', new_card_url, class: 'subscribe-link-alert' %>
              to continue after the trial period.
            <% end %>
          </div>
        <% elsif !current_user.is_active? %>
          <div class="alert alert-info alert-dismissable">
            <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
            Start fishing like a pro! Checkout our <%= link_to 'Heatmap',maps_url %> feature. <%= link_to 'Add card details', new_card_url, class: 'subscribe-link-alert' %></div>
        <% end %>
      <% end %>
      <% if flash.any? %>
        <div class="container">
          <% flash.each do |key, value| %>
            <div class="alert alert-<%=key%>">
              <%= value.html_safe %>
            </div>
          <% end %>
        </div>
      <% end unless devise_controller? %>
      <%= yield %>
    </div>
  </body>
</html>
